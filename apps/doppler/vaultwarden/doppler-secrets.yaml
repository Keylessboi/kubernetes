apiVersion: ://secrets.com
kind: DopplerSecret
metadata:
  name: web-app-doppler-sync
  namespace: default
spec:
  tokenSecret:
    name: vaultwarden-doppler-token
  managedSecret:
    name: vaultwarden-secrets
    namespace: vaultwarden
